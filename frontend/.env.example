# URL base del backend (sin trailing slash).
# En local apunta a uvicorn; en producción al host/dominio del API.
VITE_API_BASE_URL=http://localhost:8000
# Ejemplo producción:
# VITE_API_BASE_URL=https://api-analytics.hibot.us
